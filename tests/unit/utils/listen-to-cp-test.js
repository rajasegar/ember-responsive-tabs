import listenToCp from 'dummy/utils/listen-to-cp';
import { module, test } from 'qunit';

module('Unit | Utility | listen to cp');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = listenToCp();
  assert.ok(result);
});
